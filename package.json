{
  "name": "slide-cut",
  "version": "1.0.0",
  "description": "A small desktop slide compositor for arranging images, text, and simple vector-like marks.",
  "author": "seungjae",
  "license": "MIT",
  "scripts": {
    "start": "tauri dev",
    "tauri": "tauri",
    "build": "tauri build --bundles app && npm run package:release",
    "build:app": "tauri build --bundles app",
    "build:mac": "npm run build",
    "build:dmg": "tauri build --bundles dmg",
    "package:release": "./scripts/package-release.sh",
    "build:win": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  },
  "dependencies": {
    "lucide": "^1.16.0",
    "pretendard": "^1.3.9"
  }
}
